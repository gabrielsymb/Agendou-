# Exemplo de variáveis de ambiente para frontend
# Valor padrão para desenvolvimento local
PUBLIC_API_BASE=http://localhost:3000
# PRIVATE_API_BASE é usado apenas no server-side (endpoints proxy) e NÃO deve ser commitado com credenciais reais
# Em dev pode apontar para o backend local também
PRIVATE_API_BASE=http://localhost:3000
